import { FormContainerDirective } from './form-container.directive';

describe('FormContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormContainerDirective();
    expect(directive).toBeTruthy();
  });
});
