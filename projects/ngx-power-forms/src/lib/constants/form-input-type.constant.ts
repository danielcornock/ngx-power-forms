export enum FormInputType {
  TEXT = 'text',
  EMAIL = 'email',
  DATE = 'date',
  NUMBER = 'number',
  SELECT = 'select',
  MULTI_SELECT = 'multiSelect',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
  TEXTAREA = 'textarea',
  DATETIME = 'datetime-local',
  MONTH = 'month'
}
